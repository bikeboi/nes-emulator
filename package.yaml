name: nes
version: 0.1.0.0
maintainer: bikeboi

dependencies:
  - base
  - bytestring
  - vector
  - mtl
  - transformers
  - containers
  - stm

  # Graphics
  - sdl2
  - linear

executables:
  nes:
    main: Main.hs
    source-dirs: src
    other-modules:
      # CPU
      - CPU
      - CPU.ROM
      - CPU.Internal
      - CPU.OpCode
      - CPU.Decode
      - CPU.Debug

      # PPU
      - PPU.Render

      # Memory
      - Memory

      - Util

tests:
  spec:
    main: "Spec.hs"
    source-dirs:
      - src
      - test
    dependencies:
      - hspec
      - QuickCheck
